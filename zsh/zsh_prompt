update_uv_env() {
    if [ -f .venv/bin/activate ]; then
        source .venv/bin/activate
        echo "Virtual environment activated: ${PWD##*/}"
    elif [ ! -f .venv/bin/activate ]; then 
        deactivate 2>/dev/null || true
    fi
}

chpwd_functions+=(update_uv_env)
